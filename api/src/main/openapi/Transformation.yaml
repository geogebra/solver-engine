title: Transformation
type: object
required:
  - path
  - fromExpr
  - toExpr
  - pathMappings
properties:
  planId:
    type: string
  path:
    type: string
  fromExpr: { }
  toExpr: { }
  pathMappings:
    type: array
    items:
      $ref: 'PathMapping.yaml'
  explanation:
    $ref: 'Metadata.yaml'
  skills:
    type: array
    items:
      $ref: 'Metadata.yaml'
  steps:
    type: array
    items:
      $recursiveRef: '#'
