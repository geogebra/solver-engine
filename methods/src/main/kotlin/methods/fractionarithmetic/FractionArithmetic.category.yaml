metadata:
  name: FractionArithmetic
  title: Fraction Arithmetic

explanations:
  SimplifyExpressionInBrackets:
    defaultTranslation: Simplify expression in brackets
  SimplifyNegativeInDenominator:
    defaultTranslation: Move negative denominator sign out of fraction in %1
    parameters:
      - fraction with negative denominator
  SimplifyNegativeInNumerator:
    defaultTranslation: Move negative numerator sign out of fraction in %1
    parameters:
      - fraction with negative numerator
  SimplifyNegativeInNumeratorAndDenominator:
    defaultTranslation: Cancel negative signs in numerator and denominator of %1
    parameters:
      - fraction with negative numerator and denominator
  ConvertIntegerToFraction:
    defaultTranslation: Write %1 as a fraction with denominator 1
    parameters:
      - integer
  AddLikeFractions:
    defaultTranslation: Add fractions %1 and %2 with the same denominator
    parameters:
      - first fraction
      - second fraction
  SubtractLikeFractions:
    defaultTranslation:
  BringToCommonDenominator:
    defaultTranslation:
  MultiplyFractions:
    defaultTranslation: Multiply the fractions %1 and %2
  FindCommonFactorInFraction:
    defaultTranslation:
  SimplifyFractionToInteger:
    defaultTranslation:
  SimplifyDividingByAFraction:
    defaultTranslation:
  TurnDivisionToFraction:
    defaultTranslation:
  SimplifyFractionWithFractionDenominator:
    defaultTranslation:
  SimplifyFractionWithFractionNumerator:
    defaultTranslation:
  DistributeFractionPositivePower:
    defaultTranslation:
  SimplifyFractionNegativePower:
    defaultTranslation:
  SimplifyFractionToMinusOne:
    defaultTranslation:
  TurnIntegerToMinusOneToFraction:
    defaultTranslation:
  TurnNegativePowerOfIntegerToFraction:
    defaultTranslation:
  ConvertMultiplicationOfFractionsToFraction:
    defaultTranslation:

  SimplifyFraction:
    defaultTranslation:
  EvaluateFractionSum:
    defaultTranslation:
  EvaluateProductsInNumeratorAndDenominator:
    defaultTranslation: Evaluate the products in the numerator and the denominator
  EvaluateSumInNumerator:
    defaultTranslation: Evaluate the sum in the numerator
  EvaluatePositiveFractionProduct:
    defaultTranslation:
  EvaluatePowerOfFraction:
    defaultTranslation:
  NormalizeSignsInFraction:
    defaultTranslation:
  NormalizeFractionsAndDivisions:
    defaultTranslation:
