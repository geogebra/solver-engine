metadata:
  name: General
  title: General Techniques
explanations:
  # Rules
  ReplaceInvisibleBrackets:
    defaultTranslation: "Add brackets to make the expression clearer"
    parameters: [ "fragment that needs brackets" ]
  RemoveBracketSumInSum:
    defaultTranslation: "Brackets around a sum term which is itself a sum can be removed"
    parameters: [ "inner sum" ]
  RemoveBracketProductInProduct:
    defaultTranslation: "Brackets around a product term which is itself a product can be removed"
    parameters: [ "inner product" ]
  RemoveBracketAroundSignedIntegerInSum:
    defaultTranslation: "Brackets can be removed from a negative term in a sum"
    parameters: [ "negative term" ]
  RemoveBracketAroundUnsignedInteger:
    defaultTranslation: "Brackets around a positive integer can be removed"
    parameters: [ "positive integer" ]
  RemoveRedundantBracket:
    defaultTranslation: "Redundant bracket can be removed"
  RemoveRedundantPlusSign:
    defaultTranslation: "Remove redundant plus sign"
  EliminateOneInProduct:
    defaultTranslation: "A factor of 1 can be eliminated from a product"
    parameters: [ "factor 1" ]
  EliminateZeroInSum:
    defaultTranslation:
  EvaluateProductContainingZero:
    defaultTranslation: "Any product containing a %1 is zero"
  SimplifyDoubleMinus:
    defaultTranslation:
  SimplifyProductWithTwoNegativeFactors:
    defaultTranslation:
  MoveSignOfNegativeFactorOutOfProduct:
    defaultTranslation:
  CancelCommonTerms:
    defaultTranslation:
  SimplifyUnitFractionToOne:
    defaultTranslation: "Simplify unit fraction to one"
  SimplifyFractionWithOneDenominator:
    defaultTranslation: "Simplify fraction with one in the denominator"
  CancelDenominator:
    defaultTranslation:
  AddClarifyingBrackets:
    defaultTranslation:
  FactorMinusFromSum:
    defaultTranslation:
  SimplifyProductOfConjugates:
    defaultTranslation:
  DistributePowerOfProduct:
    defaultTranslation:
  ExpandBinomialSquared:
    defaultTranslation:
  RewriteDivisionAsFraction:
    defaultTranslation:
  NormalizeExpression:
    defaultTranslation: Normalize the expression

  # Plans
  ReplaceAllInvisibleBrackets:
    defaultTranslation: