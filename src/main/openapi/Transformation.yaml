title: Transformation
type: object
required:
  - path
  - fromExpr
  - toExpr
  - pathMappings
properties:
  path:
    type: string
  fromExpr:
    type: string
  toExpr:
    type: string
  pathMappings:
    type: array
    items:
      $ref: 'PathMapping.yaml'
  explanation:
    $ref: 'Metadata.yaml'
  skills:
    type: array
    items:
      $ref: 'Metadata.yaml'
  steps:
    type: array
    items:
      $recursiveRef: '#'
