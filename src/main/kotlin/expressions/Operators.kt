package expressions

enum class UnaryOperator {
    Bracket,
    Minus,
    SquareRoot,
    NaturalLog,
}

enum class BinaryOperator {
    Fraction,
    Divide,
    Power,
    Root,
    MixedNumber,
}

enum class NaryOperator {
    Sum,
    Product,
}